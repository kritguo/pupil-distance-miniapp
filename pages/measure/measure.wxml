<view class="page capture-mode">
  <!-- 全屏相机 -->
  <view class="camera-fullscreen">
    <camera class="camera" device-position="{{cameraPosition}}" flash="off"></camera>

    <!-- 顶/底 深色蒙版，保证文字可读 -->
    <view class="scrim scrim-top"></view>
    <view class="scrim scrim-bottom"></view>

    <!-- ===== 顶部 ===== -->
    <view class="cap-top">
      <view class="top-row">
        <view class="btn-icon" bindtap="goBack">
          <text class="icon-arrow">‹</text>
        </view>
        <view class="shot-progress {{measureMode === 'precision' ? 'precision' : ''}}">
          <text wx:if="{{measureMode === 'precision'}}" class="mode-status">卡片精准测量</text>
          <text class="shot-progress-text">第 {{shotIndex < totalShots ? shotIndex + 1 : totalShots}} / {{totalShots}} 张</text>
          <view class="shot-dots">
            <view class="shot-dot {{shotIndex >= 1 ? 'on' : ''}}"></view>
            <view class="shot-dot {{shotIndex >= 2 ? 'on' : ''}}"></view>
            <view class="shot-dot {{shotIndex >= 3 ? 'on' : ''}}"></view>
          </view>
        </view>
        <view class="btn-icon-placeholder"></view>
      </view>
    </view>

    <!-- ===== 人脸引导框（含引导语，整体居中偏下，贴合手持时脸的位置） ===== -->
    <view class="face-guide">
      <view class="guide-text">
        <view class="guide-main">{{modeCopy.main}}</view>
        <view class="guide-sub">{{modeCopy.sub}}</view>
      </view>
      <view class="face-outline {{measureMode === 'precision' ? 'precision' : ''}}">
        <view class="eye-line"></view>
        <view class="eye-mark left"></view>
        <view class="eye-mark right"></view>
        <view wx:if="{{precisionModeEnabled && measureMode === 'precision'}}" class="card-outline" style="{{precisionCardGuideStyle}}">
          <view class="card-corner tl"></view>
          <view class="card-corner tr"></view>
          <view class="card-corner bl"></view>
          <view class="card-corner br"></view>
          <text class="card-label">卡片放这里</text>
        </view>
      </view>
    </view>

    <!-- ===== 底部 ===== -->
    <view class="cap-bottom">
      <view class="capture-guide">{{captureNotice || '对准后点击拍照'}}</view>
      <view class="capture-bottom">
        <view class="btn-round" bindtap="switchCamera">
          <text class="icon-switch">⟳</text>
        </view>
        <view class="btn-capture" bindtap="takePhoto">
          <view class="btn-capture-inner"></view>
        </view>
        <view class="btn-round" bindtap="onHelp">
          <text class="icon-help">?</text>
        </view>
      </view>
    </view>
  </view>
</view>
